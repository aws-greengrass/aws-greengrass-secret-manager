---
services:
  main:
    lifecycle:
      install:
        all: echo All installed
    dependencies:
      - aws.greengrass.secret.manager

  aws.greengrass.secret.manager:
    parameters:
      awsCloud: "bad config"